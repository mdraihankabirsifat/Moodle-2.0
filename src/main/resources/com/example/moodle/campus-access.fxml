<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.moodle.controller.CampusAccessController">

    <!-- ================= TOP NAV ================= -->
    <top>
        <HBox spacing="15" alignment="CENTER_LEFT" styleClass="top-bar" style="-fx-padding: 15;">
            <Button text="⌂ Home" styleClass="nav-btn" onAction="#goHome" style="-fx-font-size: 13px; -fx-padding: 8 15 8 15;"/>
            <Button text="My Campus" styleClass="nav-btn" onAction="#goToCampus" style="-fx-font-size: 13px; -fx-padding: 8 15 8 15;"/>
        </HBox>
    </top>

    <center>
        <StackPane>

            <VBox spacing="15"
                  alignment="CENTER"
                  maxWidth="420"
                  styleClass="card">

                <Label text="Campus Access"
                       styleClass="title-label"/>

                <fx:define>
                    <ToggleGroup fx:id="roleGroup"/>
                </fx:define>

                <HBox spacing="25" alignment="CENTER" style="-fx-padding: 10 0 10 0;">
                    <RadioButton fx:id="studentRadio" text="Student" toggleGroup="$roleGroup"/>
                    <RadioButton fx:id="teacherRadio" text="Teacher" toggleGroup="$roleGroup"/>
                    <RadioButton fx:id="authorityRadio" text="Authority" toggleGroup="$roleGroup"/>
                </HBox>

                <!-- Student fields -->
                <VBox fx:id="studentFields" spacing="10">
                    <TextField fx:id="campusIdField" promptText="Student ID"/>
                    <PasswordField fx:id="campusPassField" promptText="Password"/>
                </VBox>

                <!-- Teacher / Authority field -->
                <VBox fx:id="staffFields" spacing="10" visible="false" managed="false">
                    <TextField fx:id="staffEmailField" promptText="Teacher Email"/>
                    <PasswordField fx:id="staffPassField" promptText="Access Password"/>
                    <Label fx:id="hintLabel" text="" style="-fx-text-fill: #5a6a7e; -fx-font-size: 11px;"/>
                </VBox>

                <Label text="The initial password is provided by your university (Pass- 1234). You can change it from your Profile section." 
                       style="-fx-text-fill: #8b949e; -fx-font-size: 11px;" wrapText="true" textAlignment="CENTER" maxWidth="350"/>

                <Button text="Enter Campus"
                        onAction="#verifyCampus"/>

                <Label fx:id="messageLabel"/>

            </VBox>

        </StackPane>
    </center>

    <!-- ================= BOTTOM NAV ================= -->
    <bottom>
        <HBox alignment="CENTER_RIGHT" styleClass="bottom-bar" style="-fx-padding: 15;">
            <Button text="← Back" styleClass="nav-btn" onAction="#goBack"/>
        </HBox>
    </bottom>

</BorderPane>